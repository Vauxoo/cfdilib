.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install cfdilib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cfdilib
    $ pip install cfdilib
